# java-spark-test

Some basic Apache Spark examples, using Java.



## Docker and GraalVM

mvn clean package

docker build . -t demo

docker run -p "4567:4567" --rm demo


## Build native with docker

build.sh

